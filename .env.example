# =============================================================================
# Public.com MCP Server Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# API Authentication (Required)
# -----------------------------------------------------------------------------
# Get your API secret key from: https://public.com/settings/api
# This key is used to generate access tokens for API requests.
PUBLIC_API_SECRET_KEY=your-api-secret-key-here

# -----------------------------------------------------------------------------
# Default Account ID (Optional)
# -----------------------------------------------------------------------------
# If you have multiple accounts, set your preferred default here.
# To find your account ID, run the server and use the get_accounts tool.
# Leave blank to require accountId parameter on each request.
PUBLIC_DEFAULT_ACCOUNT_ID=

# -----------------------------------------------------------------------------
# Trading Safety (IMPORTANT)
# -----------------------------------------------------------------------------
# Controls whether order placement tools are enabled.
#
# false = SAFE MODE (default)
#   - place_order, place_multileg_order, cancel_order are DISABLED
#   - All read-only tools work normally (quotes, portfolio, preflight, etc.)
#
# true = LIVE TRADING MODE
#   - Order placement tools will execute REAL trades with REAL money
#   - Only enable if you understand the risks
#
# WARNING: Setting this to "true" allows the AI to place real trades!
ENABLE_TRADING=false
